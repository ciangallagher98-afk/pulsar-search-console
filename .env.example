# Optional — defaults to https://trac.pulsarplatform.com/graphql if unset.
# There is no shared PULSAR_API_KEY: each user signs in with their own
# Pulsar API key at /login, stored in a per-browser session cookie.
PULSAR_API_URL=
